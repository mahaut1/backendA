export const register = async (req, res) => {
  res.json({ message: "register OK – à implémenter" });
};

export const login = async (req, res) => {
  res.json({ message: "login OK – à implémenter" });
};
